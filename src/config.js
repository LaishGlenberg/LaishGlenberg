// List the repo names you want to feature in the right column.
// These will show up larger with a README preview.
const FEATURED_REPOS = [
  "deepseek-v4-for-copilot",
  "firebase-emulator-mcp",
  "arrowhead-scaler-figma-plugin",
  "termai",
  "split-image-to-pdf",
  "image-moment-react-site",
];

export default FEATURED_REPOS;
